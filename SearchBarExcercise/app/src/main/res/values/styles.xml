<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:searchViewStyle">@style/MySearchViewStyle</item>-->
    </style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:queryBackground">@color/orange</item>
        <item name="android:alpha">0.5</item>
        <item name="android:closeIcon">@drawable/cancelicon</item>
        <item name="android:searchIcon">@drawable/searchindocument</item>
    </style>

</resources>
